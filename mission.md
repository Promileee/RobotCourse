本项目主要需要实现以下一个流程：
1. 调用`QR_code_recognize_v1.py`识别二维码，读取顺序。具体数字与颜色的对应关系可参考程序中的内容
2. 调用`rect_recognize.py`识别目标圆柱体物料。注意我的用词，这是物料。
3. 识别靶标和激光点，相应程序为`target_recognize_v2.py`和`laser_recognize_v3.py`
4. 初始化开环控制，采样并训练神经网络
5. 按照二维码读到的顺序，用开环控制，先指向第一个颜色的物料，再指向第一个颜色的靶标，然后指向第二个颜色的物料，再指向第二个颜色的靶标。之后第三个